# Local / OS
.DS_Store
Thumbs.db
desktop.ini

# Print assets (keep offline)
qr-codes/

# Raw AI-generated source material — only the curated copies inside media/
# are actually used by the site; this folder is just the working stash.
all images/
audio-scripts/*.wav

# Editor
.vscode/
.idea/

# Python helper
__pycache__/
*.pyc
_tmp.webp
